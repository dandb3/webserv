/**
 * 민관이형이 구현한 utils.hpp에 머지할 예정.
 * 그냥 임시파일이다.
*/
#ifndef ENG
#define ENG

#include <string>
#include <netinet/in.h>

static std::string sizeToStr(size_t size);
static std::string ft_inet_ntoa(in_addr_t addr);

#endif